{
  "name": "extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite build --config vite.content.config.ts && vite build --config vite.inject.config.ts",
    "build:dev": "tsc && vite build --mode development && vite build --config vite.content.config.ts --mode development && vite build --config vite.inject.config.ts --mode development",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "chrome-types": "^0.1.429"
  }
}
